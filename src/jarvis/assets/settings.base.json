{
  "$comment": "Jarvis OS baseline Claude Code settings, injected into every managed project. Project-specific needs belong in the catalog's settings_overrides, not here and not in the project. `outputStyle` reaches user sessions and workers alike because both settings files build from this one — docs/superpowers/specs/2026-08-22-agent-concision.md SS3.",
  "env": {
    "JARVIS_MANAGED": "1"
  },
  "outputStyle": "Concise",
  "permissions": {
    "allow": ["Bash(jarvis *)"]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Edit|Write|NotebookEdit",
        "hooks": [
          { "type": "command", "command": "__JARVIS_HOOK_CMD__", "timeout": 30 }
        ]
      }
    ],
    "PostToolUse": [
      {
        "$comment": "Two jobs: mirror Claude Code's own memory files into the OS knowledge base, and re-assert the work-order record on the first tool call after a compaction. Unmatched (every tool) for the second one — a compacted worker's next call is usually Bash, and waiting for a file edit would leave it running blind until it made one. Costs one stat() per tool call until a PreCompact arms it.",
        "hooks": [
          { "type": "command", "command": "__JARVIS_HOOK_CMD__", "timeout": 30 }
        ]
      }
    ],
    "PreCompact": [
      {
        "$comment": "Arms the re-assertion above. PreCompact cannot inject context and neither can PostCompact, so this only records and flags; PostToolUse delivers.",
        "hooks": [
          { "type": "command", "command": "__JARVIS_HOOK_CMD__", "timeout": 30 }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "__JARVIS_HOOK_CMD__", "timeout": 30 }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "__JARVIS_HOOK_CMD__", "timeout": 30 }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          { "type": "command", "command": "__JARVIS_HOOK_CMD__", "timeout": 30 }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          { "type": "command", "command": "__JARVIS_HOOK_CMD__", "timeout": 30 }
        ]
      }
    ]
  }
}
